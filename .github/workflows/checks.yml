name: checks
on: [push]
jobs:
  php-unit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: php-actions/composer@v5
        with:
          dev: yes
          php_version: 7.3
          version: 1
      - run: php --version
      - run: composer --version
